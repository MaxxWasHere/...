# Host: laptop — replace with output from nwg-displays (or hand-written monitor= / workspace= lines).
#
# Example built-in panel only:
# monitor=,preferred,auto,1.0
# workspace=1,monitor:eDP-1,default:true
#
# After editing: hyprctl reload
