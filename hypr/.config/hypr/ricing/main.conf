# Non–end-4 defaults: window rules, extra layers, waffle quickshell, background fixes
# Paths are relative to this file's directory (~/.config/hypr/ricing/), not hyprland.conf's cwd.
source=rules.conf
source=layers.conf
